/* A function that returns the sum of two numbers.
Take the two arguments of the function,
Create a new variable to store the sum
Add the two arguments together and store it in the sum
Return the sum

START

Given two numbers to find the sum of

SET sum

sum = argument1 + argument2

PRINT sum

*/


/*  a function that takes an array of strings,
and returns a string that is all those strings concatenated together

Given an array of strings.

Create an empty string variable.
Iterate through each item in the array of strings,
and add its value to the string variable.

Return the string variable.

START
Given an array of strings.

SET combinedString = empty string
SET iterator = 1

WHILE iterator < length of array
  combinedString = combinedString + string in the array
  iterator = iterator + 1

PRINT combinedString
*/

/* a function that takes an array of integers,
and returns a new array with every other element

Given an array of integers.
Create a new empty array to hold the desired elements.
Iterate through the given array. 
If the index number of the element is an even number,
then push the value of the element to the end of the empty array.
Return the array.

START
Given an array of integers.

SET everyOtherElement = empty array
SET indexNumber = 0

WHILE indexNumber < length of array
  IF indexNumber is even
    add the element to the end of the everyOtherElement array
    
PRINT everyOtherElement
*/
